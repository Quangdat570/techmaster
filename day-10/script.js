// prompt - 
// 1- nếu ng dùng bấm cancel=null
// 2- kết quả trả về khi ng dùng nhập giá trị => string
let input = prompt("vui lòng nhập mã",0);

if (input % 2 == 0) {
    console.log("so chan")
}
else {
    console.log ("so le")
}
if ( condision) {
    // mang áo mưa
} else if (condision) { // trời nắng
    // mang mũ
} else if (condision) { // trời âm u
    // mang cả áo lẫn mũ
} else {
    // không mang gì hết
}


if ( isWeenkend) { // nếu cuối tuần
    if (haveMoney) { // nếu có tiền
       if (haveGirlFriend) {// nếu có người yêu
            console.log("rủ người yêu đi chơi");
       } esle {
        console.log("rủ bạn đi nhậu");
       }
    } else {// không có tiền
        console.log("ở nhà");
} 
} esle { //không phải cuối tuấn
    console.log("đi làm");
}

if (isWeenkend || isHoliday) {
    if (haveMoney $$ haveGirlFriend) console.log("rủ người yêu đi xem phim");
    else if (haveMoney) console.log("rủ bạn đi nhậu");
    else console.log("ở nhà");
} else console.log("đi làm");


// khi mà điều kiện không liên quan đến nhau => tách riêng ra từng if else
// khi điều kiện liên quan đến nhau => kiểm tra điều kiện nhỏ nhất ( biến/tập giá trị nhỏ nhất)
// kết hợp các điều kiện cho cấu trúc gọn gàng/tối ưu
// nếu condision là biến thì tính toán trước => gán vào biến => dễ đọc